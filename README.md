Лабораторная работа на тему "Система управления доступом" по дисциплине "Моделирование безопасных компьютерных систем"

Система управления доступом пользователей к символам через графический интерфейс

Теория:

Модель управления доступом
Система реализует модель дискреционного управления доступом (DAC), где:
- Субъекты - пользователи системы
- Объекты - символы (буквы, цифры, специальные знаки)
- Права доступа - связи между субъектами и объектами

Принципы безопасности
- Аутентификация - проверка существования пользователя в БД
- Авторизация - контроль доступа только к разрешённым символам
- Изоляция данных - разделение прав через реляционную модель
  
О проекте

Система состоит из двух приложений на Go с GUI (Fyne):

Пользовательское приложение - для работы с разрешёнными символами

Админ-панель - для управления пользователями и правами доступа

Оба приложения взаимодействуют через единую базу данных SQLite, демонстрируя принципы многозвенной архитектуры.

Система может быть расширена для решения реальных задач контроля доступа в корпоративных средах.

Функциональность:

В ходе работы были написаны две программы: для пользователя и для 
администратора системы. Программа администратора представлена на 
рис. 1 и рис. 2.

Программа позволяет создавать и удалять субъекты и 
объекты как по одному, так и списком. Так же администратор имеет 
возможность выдать или удалить права на определенные объекты как 
одному, так и нескольким субъектам сразу.  

<img width="579" height="401" alt="image" src="https://github.com/user-attachments/assets/93dba729-06c3-4835-9a0a-ef9ee34be3c0" />

Рис.1 Программа администратора 

<img width="590" height="410" alt="image" src="https://github.com/user-attachments/assets/b4677744-547d-46ff-bedc-e97c1bdb88e0" />

Рис.2 Программа администратора 

Программы администратора и пользователя общаются через базу данных 
SQLite, что хорошо подходит для модели взаимодействия многие-ко-многим.

Все изменения, вносимые администратором обновляют содержимое базы 
данных, что можно увидеть в интерфейсе программы администратора. 

На рис. 3 представлено окно, которое открывается при запуске 
приложения пользователя. При вводе данных приложение проверяет, 
существует ли пользователь с таким именем в базе данных, если существует, то 
после ввода открывается следующее окно (рис. 4). 

<img width="461" height="332" alt="image" src="https://github.com/user-attachments/assets/657b22e9-8def-4cd2-af8b-a398a057d10d" />

Рис.3 Программа пользователя 

<img width="456" height="363" alt="image" src="https://github.com/user-attachments/assets/cf6f198b-b7f0-4020-93c1-851885c7b068" />

Рис.4 Программа пользователя 

 
Обе программы успешно работают вместе, что достигается обновлением 
интерфейса приложения данными из базы данных при каждом действии 
администратора или пользователя в программе, при этом программа 
пользователя даже при отсутствии действий обновляет данные, получая их из 
базы данных каждые 2 секунды.

Технологический стек:
Компонент	Технология
Язык	Go 1.25.3
GUI Framework	Fyne
База данных	SQLite
Архитектура	Multi-tier
Взаимодействие	Shared Database

Структура базы данных:

users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,  
    name TEXT NOT NULL UNIQUE              
)

letters (
    id INTEGER PRIMARY KEY AUTOINCREMENT,  
    char TEXT NOT NULL UNIQUE              
)

user_letters (
    user_id INTEGER,                      
    letter_id INTEGER,                    
    PRIMARY KEY (user_id, letter_id),     
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (letter_id) REFERENCES letters(id)
)
